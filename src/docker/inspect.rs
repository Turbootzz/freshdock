// Phase 2 will implement full container-spec capture for the recreate cycle.
// Stubbed here so the module is wired into the tree without lying about its
// capabilities.

#[allow(dead_code)]
pub async fn inspect() {
    unimplemented!("inspect lands in Phase 2 (P2-1)")
}
